# Database connection
DATABASE_URL=postgresql://blackledger@localhost/blackledger

# Server configuration
PORT=8000

# Authentication (optional)
AUTH_ENABLED=false
# JWKS_URL=http://localhost:8080/realms/blackledger/protocol/openid-connect/certs

# Logging
RUST_LOG=blackledger=debug,tower_http=debug,sqlx=warn